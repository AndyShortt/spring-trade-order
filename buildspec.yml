version: 0.2

phases:
  build:
    commands:
       - gradle build
artifacts:
  files:
    - build/libs/spring-trade-service-0.1.0.jar
  secondary-artifacts:
    artifact2:
      files:
        - scripts/*
    artifact3:
      files:
        - appspec.yml
