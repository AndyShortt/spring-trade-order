version: 0.2

phases:
  build:
    commands:
       - gradle build
artifacts:
  files:
    - build/libs/spring-trade-service-0.1.0.jar
    - scripts/*
    - appspec.yml
    - spring-trade-service.service
